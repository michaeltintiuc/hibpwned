version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.9

    working_directory: /go/src/github.com/michaeltintiuc/hibpwned
    steps:
      - checkout
      - run: go get -v -t -d ./...
      - run: ./test.sh
      - run: bash <(curl -s https://codecov.io/bash)
